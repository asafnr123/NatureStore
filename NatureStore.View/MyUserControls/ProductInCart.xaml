<UserControl x:Class="NatureStore.View.MyUserControls.ProductInCart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NatureStore.View.MyUserControls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="50" />
        </Grid.ColumnDefinitions>


        <Label Content="{Binding ProdName}" Grid.Column="0" Style="{StaticResource productLabel}"/>
        <Label Content="{Binding ProdQty}" Grid.Column="1" Style="{StaticResource labels}"/>
    </Grid>
</UserControl>
