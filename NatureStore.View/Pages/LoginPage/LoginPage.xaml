<Page x:Class="NatureStore.View.Pages.LoginPage.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NatureStore.View.Pages.LoginPage"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPage">

    <Grid>
        <Border Style="{StaticResource border}" Width="400" Height="250">
            <WrapPanel Width="300" Height="250" VerticalAlignment="Center">
                <Label Content="Username:" Style="{StaticResource labels}" Margin="20,30,0,0" />
                <TextBox Name="usernameTxt" Style="{StaticResource userInput}" Margin="-65,70,0,0" />

                <Label Content="Password:" Style="{StaticResource labels}" Margin="20,10,0,0"  />
                <PasswordBox Name="PassTxt" Style="{StaticResource passInput}" Margin="-60,50,0,0" />

                <Button Name="LoginBtn" IsDefault="True" Content="Login" Style="{StaticResource loginBtn}" Margin="20,30,0,0" Click="LoginBtn_Click"  />
                <Button Name="RegisterBtn" Content="Register" Style="{StaticResource regularBtn}" Margin="10,30,0,0" Click="RegisterBtn_Click"/>

            </WrapPanel>
        </Border>
    </Grid>
</Page>
